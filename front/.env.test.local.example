# Playwright E2E credentials — copy to .env.test.local (git-ignored) and fill in.
# The account must exist locally: sign it up once at `/signup`, then fill it with `pnpm seed` in nest-api.
E2E_EMAIL=local.dev@mock.io
E2E_PASSWORD=
